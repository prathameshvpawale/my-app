import { View, Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context"


export default function explore() {
    return <SafeAreaView>
        <Text>
            this is a explorar page
        </Text>
    </SafeAreaView>
}